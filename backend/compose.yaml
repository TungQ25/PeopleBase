services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=${POSTGRES_DB:-peoplebase}'
      - 'POSTGRES_PASSWORD=${DB_PASSWORD:-peoplebase}'
      - 'POSTGRES_USER=${DB_USERNAME:-peoplebase}'
    ports:
      - '${DB_PORT:-5432}:5432'
